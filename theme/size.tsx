import { defaultComponentSize } from "@/utils/constants";
import { withDefaultSize } from "@chakra-ui/react";

const size = withDefaultSize({ size: defaultComponentSize });

export default size;
